{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window — core APIs plus SQL plugin (lnreader.db).",
  "windows": ["main"],
  "permissions": ["core:default", "sql:default"]
}
