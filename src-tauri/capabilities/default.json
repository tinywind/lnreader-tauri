{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions: core + sql + http. App/repository fetches use a permissive HTTPS/HTTP scope; plugin-owned site fetches use the scraper WebView.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default",
    "sql:allow-execute",
    "deep-link:default",
    "dialog:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://**" },
        { "url": "http://**" },
        { "url": "https://**:*/**" },
        { "url": "http://**:*/**" }
      ]
    },
    {
      "identifier": "shell:allow-open",
      "allow": [
        { "url": "https://**" },
        { "url": "http://**" },
        { "url": "https://**:*/**" },
        { "url": "http://**:*/**" }
      ]
    }
  ]
}
