#!/bin/sh
set -eu

echo "Running pre-push checks..."

HOOK_DIR=$(dirname "$0")
"$HOOK_DIR/pre-commit"

if ! command -v cargo >/dev/null 2>&1; then
  echo "cargo is required for pre-push checks."
  exit 1
fi

cargo check --manifest-path src-tauri/Cargo.toml
